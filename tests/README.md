# Tests

This directory contains test files for the SeedSigner project.

## Running Tests

To run the tests:

```bash
pytest
```

To run tests with coverage:

```bash
pytest --cov=seedsigner --cov-report=html
```
